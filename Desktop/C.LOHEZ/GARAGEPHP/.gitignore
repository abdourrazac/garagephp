# Fichiers de configuration sensibles et spécifiques à l'environnement
.env
.env.*.local
.env.production.local
.env.staging.local
.env.developement.local

# Dévelopement du gestionnaires du paquets PHP (composer)
/vendor/
/composer.lock # Souvent inclus pour des buikds reproductibles, mais ignore si vous régénérez Souvent

# logs de l'application 
/storage/*.log
/storage/logs/
/storage/cache/
/storage/framework/
/storage/app/

#Fichiers de cache et temporaires générés par l'application
/cache/
/tmp/
*.tmp
*.temp
*.bak
*.swp
*.swo

# Fichiers de session PHP
/sessions/

# Fichiers d'IDE et d'editeurs de texte
.idea/              # JetBrains (PhpStorm, IntelliJ, etc.)
.vscode/            # VS code (extensions, parametres de workspace)
*.sublime-project
*.sublime-workspace
.DS_Store           # macOS
Thumbs.db          # Windows
ehthumbs.db 
.sass-cache/       # cache sass/SCSS
*.log              # Fichiers de log génériques        
*.sqlite           # Fichiers de base de données SQLite ( si utilisé)

# Fichiers de build ou de distribution
/build/
/dist/

# Fichiers docker ignorés (si non essentiels au build ou à la CI)
.dockerignore # si vous avez un .dockerignore spécifique pour le build Docker